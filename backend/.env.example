# Environment
NODE_ENV=

# Server
PORT=

# MongoDB (LOCAL — you installed it)
MONGODB_URI=

# Auth
JWT_SECRET=
JWT_EXPIRES_IN=

# Mail (works but move to App Password later)
BREVO_SMTP_HOST=
BREVO_SMTP_PORT=

BREVO_SMTP_USER=

MAIL_FROM_NAME=
MAIL_FROM_EMAIL=

BREVO_SMTP_PASS=
# PostgreSQL (external — fine)
POSTGRESQL_DATABASE_URL=